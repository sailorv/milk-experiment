import React from "react"

import "../styles/styles.scss"


const BytesPage = () => {

    return (
<div style={{height: `100vh`, display: `flex`, alignItems: `center`, textAlign: `center`, width: `100%`}}>
  <section id="page-header" style={{textAlign: `center`, width: `100%`}}>
    <div id="page-title">
      www.milk-experiment.net
    </div><span className="cursor"></span>
  </section>
</div>
    )
}

export default BytesPage